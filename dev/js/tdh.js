/* individuelles JS */

var TDH = {
    /**
     * Colja
     */
    initAkkordeon: function () {
        "use strict";
        // do whatever needed
    }
};

/*
 When DOM is ready
 */
$(function () {
    // Akkordeon
    if ($('#akkordeon').length > 0) {
        TDH.initAkkordeon();
    }
});